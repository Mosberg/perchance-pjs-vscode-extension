# MANIFEST

Generated: 2026-09-16T18:36:48.601Z

## 01-internal-code
- main.pjs   (841 bytes, 13 lines) — exports the `$output(speed)` function that injects rotating CSS animations into the page.
- index.html (1470 bytes, 58 lines) — plugin documentation page + demo button.

## 02-external-dependencies
- NONE. main.pjs contains no `{import:...}` statements.

## 03-third-party-assets
- NONE. No images, audio, models, shaders, animations, or JSON data are referenced.

## 04-project-resources
- NONE. There is no `src/` tree.

## 05-build-config
- NONE. Perchance generators have no build pipeline; source is served directly.

## Verification
- main.pjs: 1 top-level definition (`$output(speed)`).
- index.html: plain HTML + <style>, no <script> tags, no external URLs.
