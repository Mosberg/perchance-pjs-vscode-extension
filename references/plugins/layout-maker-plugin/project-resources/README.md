# Project resources: NONE AS FILES

All layouts, data and UI are inline: pjs lists in main.pjs, HTML and a single <style> block in index.html.
There is no JSON, spritesheet, level data or assets folder.

This directory is intentionally present in the archive only to
make the empty category explicit.