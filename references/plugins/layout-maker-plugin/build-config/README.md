# Build / config files: NONE

Perchance generators have no build pipeline - no package.json, bundler, lockfile, CI or toolchain.
Configuration lives inside main.pjs as literal top-level items, and $meta is not defined.

This directory is intentionally present in the archive only to
make the empty category explicit.