# Third-party assets: NONE

This project uses no images, audio, models, shaders, animations, fonts or icons.
There is no src/ directory and nothing is fetched at runtime.

This directory is intentionally present in the archive only to
make the empty category explicit.