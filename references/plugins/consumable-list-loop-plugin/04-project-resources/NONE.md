# Project resources: NONE

The project generates no data resources at runtime either. There is no
`src/` file tree, no JSON data files, no generated/cached artifacts, and no
binary blobs. The plugin operates purely on whatever Perchance list the caller
passes into `consumableListLoop(...)` at runtime.

All content the project "has" is the two files in `../01-internal-code/`.
