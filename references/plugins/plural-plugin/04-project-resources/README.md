# (none)

This generator ships no binary or creative assets. Nothing in this category is referenced
by `main.pjs` or `index.html`.

Verified by scanning both source files for `img`/`audio`/`video`/`source`/`link`/`font`/
`url()`/`fetch(`/import references: the only external URIs present are ordinary hyperlinks
in the docs UI (`https://perchance.org/plural-plugin-example#edit` and `/plugins`).
