# External code / dependencies

NONE.

This generator has zero external dependencies:

- No `{import:...}` Perchance plugin imports (verified: 0 matches in main.pjs).
- No <script> tags (verified: 0 in index.html).
- No npm packages, no CDN module imports, no bundler, no package.json.

The only runtime is the Perchance engine itself, which is supplied by the
platform and is therefore not included in this package.
