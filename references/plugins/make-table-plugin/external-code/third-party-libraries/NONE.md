No third-party libraries are used.

This generator has zero {import:...} statements, zero npm dependencies and zero
CDN dependencies. The only external code ever loaded is the Perchance engine
itself (see ../platform-runtime/).
