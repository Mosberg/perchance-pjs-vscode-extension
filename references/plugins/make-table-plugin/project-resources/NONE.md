No project resources exist.

No images, audio, models, textures, shaders, animations, JSON/data files,
fonts, prefabs, or UI resource files are used by or generated for this
generator. The entire UI is emitted as HTML strings by main.pjs at render time.
