No build or configuration system exists.

No package.json, lockfile, bundler (esbuild/webpack/vite/rollup), transpiler,
minifier, test runner, linter config, CI workflow, or .env file. main.pjs and
index.html are shipped exactly as authored and interpreted by the Perchance
platform at page render time.
