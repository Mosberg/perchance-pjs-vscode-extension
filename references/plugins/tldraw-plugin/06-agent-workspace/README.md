# 06 - Agent workspace configuration

`AGENTS.md` is the instruction file the Perchance AI helper reads when working in this
workspace (platform/DSL reference, plugin docs, tooling rules). It is NOT loaded by the
generator, is not served to visitors, and has no effect on runtime behavior. It is
included only because it is the one remaining file in the workspace.