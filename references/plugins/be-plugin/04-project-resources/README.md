# Project resources

**None.**

No project-owned binary assets exist (no images/audio/models/data files).
All content is the two text files in `../01-internal-code/`.

The only data-like content is the hard-coded verb table inside `toBe()`
in `main.pjs`.
