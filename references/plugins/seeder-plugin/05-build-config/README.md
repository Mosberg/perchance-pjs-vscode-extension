# Build / configuration files

NONE.

No package.json, lockfile, bundler config, tsconfig, Makefile, Dockerfile, CI pipeline,
env file, or toolchain of any kind. Perchance's engine parses main.pjs and renders
index.html at request time — there is no build step to reproduce.
