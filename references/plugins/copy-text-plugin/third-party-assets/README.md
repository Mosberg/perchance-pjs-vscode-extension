# Third-party assets

None. This project ships no images, audio, models, shaders, fonts, sprites, or
animations. The only non-code visual is a Unicode dice glyph (U+2684 + U+FE0E variation
selector) used inline in the HTML. There are no asset files to include.
