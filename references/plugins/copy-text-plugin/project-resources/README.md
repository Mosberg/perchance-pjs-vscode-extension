# Project resources

None. This project has no external JSON data, prefabs, templates, save files, or runtime
resource bundles. The only data is the word-list content embedded directly in the
reference generators' main.pjs files (e.g. external-code/animal/main.pjs).
