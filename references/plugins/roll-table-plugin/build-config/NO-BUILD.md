# Build / config files

There is no build pipeline. A Perchance generator consists of two files pasted into the editor:

1. `main.pjs` -> the "code" panel
2. `index.html` -> the "HTML" panel

The engine renders them in the browser; no compiler, bundler, or dependency manager is involved.
