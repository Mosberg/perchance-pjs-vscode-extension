# Asset Manifest — "tap-anywhere-plugin"

Generated: full inventory of every asset in this project.

## 1. Internal code
- internal-code/main.pjs   — the plugin itself (perchance-js). Exposes $output = the tap-anywhere behavior.
- internal-code/index.html  — the generator's HTML body (description/docs page UI).

## 2. External code (imports / libraries / modules)
- NONE. This generator has ZERO {import:...} statements, no CDN <script> tags, no ES-module imports, no npm packages.

## 3. Third-party assets (images / audio / models / shaders / fonts)
- NONE.

## 4. Project resources (JSON data / prefabs / templates / UI resources)
- NONE.

## 5. Build / config files
- NONE. There is no build pipeline, bundler, package.json, lockfile, or CI config.
- (AGENTS.md is the AI workspace instruction file, not part of the shipped generator. Included at the root for completeness.)
