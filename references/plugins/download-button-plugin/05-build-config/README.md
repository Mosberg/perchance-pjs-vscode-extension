# 05 — Build / config / pipeline files

NONE.

Perchance generators have no package.json / Makefile / bundler config. The build
is: paste code into the editor -> the platform compiles and serves it. The only
"artifact" of that pipeline is captured in 04-project-resources/standalone/.
