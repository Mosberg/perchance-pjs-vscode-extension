# 03 — Third-party assets (images, audio, models, shaders, animations, fonts, data)

NONE.

The page uses no image/audio/model/shader/animation files and no remote fonts or
JSON. Visuals are pure CSS (inline <style>) plus the browser's default UI. Demo
buttons use the browser's native <button> rendering.
