# 02 — External code (third-party libraries, imports, dependencies)

NONE.

This generator has zero dependencies:
- main.pjs imports: [] (verified via perchance.org/api/getGeneratorsAndDependencies)
- No {import:...} plugin references.
- No npm packages, no CDN <script> tags, no ES-module imports.

The only external runtime it relies on is the Perchance platform itself
(the pjs engine that loads main.pjs and renders index.html), which is not a
redistributable asset of this project.
