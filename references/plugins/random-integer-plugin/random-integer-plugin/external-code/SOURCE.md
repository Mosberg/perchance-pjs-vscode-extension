# external code: perchance-engine-491bf81418aa4b69.js

Downloaded byte-exact via a CORS-free fetch of:

https://perchance.org/lib/perchance-engine-491bf81418aa4b69.js

- Bytes: 115369
- SHA-256: `491bf81418aa4b691be009ce503200880cba472d287541db1950c4b02cc221ad`
- Role: the Perchance template engine for this page. It is injected by the hosting platform (not referenced
  by this project's own code) and is the component that turns `main.pjs` into the `randomInteger`-style
  function and evaluates the `[...]` templates in `index.html`. It also supplies the global `update(el)`
  helper that the example buttons call.
- The hex string in the filename is a platform content hash; this copy is a snapshot, and the platform may
  serve a different build under a different filename later.

No other library is used. There is no npm dependency, CDN import, or bundled third-party module in this project.
