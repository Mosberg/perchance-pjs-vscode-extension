# Third-party binary assets

**None.**

This project references no external images, audio, models, fonts, sprites, shaders,
animations or prefabs. Every visual element in `index.html` is inline CSS/HTML plus
Unicode glyphs (`\u2684\uFE0E`, the die face). There is nothing to download or host.
