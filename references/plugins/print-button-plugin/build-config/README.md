EMPTY CATEGORY — no build pipeline or configuration files.

This generator is authored directly in the Perchance editor; there is no package.json, bundler, lockfile, transpiler or CI step. The Perchance platform serves index.html and evaluates main.pjs (perchance-js) at request time.
