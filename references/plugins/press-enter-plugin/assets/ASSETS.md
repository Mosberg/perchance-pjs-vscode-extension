# Assets: NONE

This generator contains **no** image, audio, video, model, shader, animation, font, JSON-data,
prefab, template, or prebuilt-UI assets.

Inventory result (exhaustive):

| Asset type | Count | Notes |
|---|---|---|
| Images / sprites / textures / icons | 0 | — |
| Audio / music / SFX | 0 | — |
| Video | 0 | — |
| 3D models / meshes | 0 | — |
| Shaders (GLSL/WGSL) | 0 | — |
| Animations / rigs | 0 | — |
| Particle effects | 0 | — |
| JSON / CSV / binary data files | 0 | — |
| Prefabs / templates / scene files | 0 | — |
| Fonts | 0 | Uses the browser default font stack only. |
| Stylesheets | 0 external | One inline `<style>` block in `index.html` (no stylesheet files). |
| Scripts (external) | 0 | One inline `<script>` string produced by `main.pjs` (no script files). |

The only "resource" of any kind is the inline CSS block at the bottom of `index.html`:

```css
	body {
			background:#eee;
		}
	code {
		background-color:#eee;
		padding:0.1em 0.2em
	}
	ul li { margin-top:0.4em; }
```
