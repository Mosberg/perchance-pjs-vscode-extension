# Project resources

None.

The project has no data files, localisation strings, level data, configuration tables,
design documents or reference art. Everything the generator needs is in
`internal-code/main.pjs` and `internal-code/index.html`.
